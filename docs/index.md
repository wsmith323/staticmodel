# StaticModel Documentation

**StaticModel** provides a framework for modeling tabular constants in Python.

Start with the user guide for the core API, primitive representations, Django
model fields, and Django REST Framework serializer fields. The rationale page
keeps the longer explanation of the problems StaticModel is intended to solve.

```{toctree}
:maxdepth: 2

User Guide <user-guide>
Why StaticModel? <rationale>
```
