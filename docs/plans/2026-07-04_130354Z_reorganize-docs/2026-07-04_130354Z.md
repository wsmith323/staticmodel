Plan-Id: 2026-07-04_130354Z-reorganize-docs
Source-Block: selected plan block 1 of 1
Execution-Summary: 2026-07-04_130354Z_execution.md

# Reorder Documentation Around Usage

## Summary
- Make the reader’s first path usage-oriented: brief introduction, install/basic example, then the full user guide.
- Move the long “why this exists / alternatives break down” explanation out of the top-level flow into a separate rationale page.
- Keep Django and DRF documentation inside the user guide as advanced integration sections.

## Key Changes
- Replace `docs/index.md` with a ReadTheDocs landing page that has a short intro and a Sphinx `toctree`:
  - `User Guide <user-guide>`
  - `Why StaticModel? <rationale>`
- Create `docs/user-guide.md` from the current `## StaticModel User Guide`, `## Django model fields`, and `## Django Rest Framework serializer fields` content.
- Create `docs/rationale.md` from the current lengthy introductory comparison, including “Using built-in types…” and “The StaticModel solution.”
- Rewrite `README.md` as a concise GitHub/PyPI entrypoint:
  - short project description
  - link to ReadTheDocs
  - small usage example or direct “start here” link
  - Markdown links to `docs/user-guide.md` and `docs/rationale.md`
- Preserve existing examples and doctest prompts unless a heading/context adjustment is required.

## Test Plan
- Build docs with `make -C docs html`.
- Run documentation doctests with `make -C docs doctest`, since current examples are executable doc snippets.
- Check README rendering manually enough to confirm GitHub-compatible links and heading order.

## Assumptions
- The long comparison remains valuable, just not as the first-reader path.
- Django and DRF are part of the user guide, not separate top-level docs pages.
- It is acceptable for PyPI’s long description to become shorter because `setup.py` uses `README.md`.
