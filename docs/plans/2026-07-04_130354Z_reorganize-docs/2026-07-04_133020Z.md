Plan-Id: 2026-07-04_133020Z-reorganize-docs
Source-Block: selected plan block 1 of 1
Execution-Summary: 2026-07-04_133020Z_execution.md

# Add README and User Guide Navigation

## Summary
- Improve documentation navigation without adding generation tooling.
- Add deep links under the README’s user-guide link.
- Add a concise manual contents list near the top of the user guide.

## Key Changes
- Update `README.md` `Documentation` section so `User Guide` is a parent item with nested links to:
  - StaticModel classes
  - Member access methods
  - Sub-models
  - Primitive collections
  - Django model fields
  - Django REST Framework serializer fields
- Remove the separate prose sentence listing user-guide contents, since the nested links will carry that information.
- Add a `## Contents` section near the top of `docs/user-guide.md`, immediately after the `# StaticModel User Guide` heading, with local links to the same major sections.
- Keep the top-level ReadTheDocs URL in `README.md` as the canonical docs link.
- Do not add TOC generation tooling or Markdown include machinery.

## Test Plan
- Run `make -C docs html SPHINXBUILD=/Users/warren/work/virtualenvs/staticmodel/bin/sphinx-build`.
- Verify README links use GitHub-compatible relative anchors.
- Verify `docs/user-guide.md` local contents links match the actual headings.

## Assumptions
- Manual duplication is acceptable because the list is short and stable.
- GitHub convenience is the main reason for README deep links.
- ReadTheDocs navigation remains handled by the Sphinx toctree/sidebar.
