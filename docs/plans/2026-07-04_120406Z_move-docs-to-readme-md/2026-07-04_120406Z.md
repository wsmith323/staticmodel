Plan-Id: 2026-07-04_120406Z-move-docs-to-readme-md
Source-Block: last-of-1
Execution-Summary: 2026-07-04_120406Z_execution.md

# Move StaticModel Documentation To README.md

## Summary
- Create root `README.md` as the canonical documentation source, combining the current `README.rst` summary with the full docs currently stored in:
  - `staticmodel/__init__.py`
  - `staticmodel/django/models/fields.py`
  - `staticmodel/django/rest_framework/serializers.py`
- Delete `README.rst`.
- Replace the long module docstrings with brief module summaries so the code no longer owns the user guide.
- Keep Read the Docs working by making Sphinx render the root `README.md`.

## Key Changes
- Convert the existing reStructuredText docstring content to Markdown:
  - Convert section adornments to `#`, `##`, and `###`.
  - Convert inline literals from double-backtick RST to Markdown backticks.
  - Convert bullet lists to Markdown bullets.
  - Preserve doctest examples as `pycon` fenced blocks.
  - Ensure doctest blocks have a blank line before closing fences so Python doctest does not treat the fence as expected output.
- Update packaging metadata:
  - `setup.py` reads `README.md`.
  - `long_description_content_type` becomes `text/markdown`.
  - `MANIFEST.in` includes `README.md` instead of `README.rst`.
- Update Sphinx/Read the Docs:
  - Add `myst-parser` to `docs/requirements.txt`.
  - Add `myst_parser` to `docs/conf.py`.
  - Allow `.md` sources in Sphinx.
  - Replace `docs/index.rst` with a Markdown Sphinx entrypoint that includes `../README.md`.
- Update doc tests:
  - Change `tests/test_staticmodel_docs.py` from `doctest.DocTestSuite(staticmodel)` to a README-backed doctest, using `doctest.DocFileSuite`.
  - Keep examples executable from the repo root context.

## Test Plan
- Run `/Users/warren/work/virtualenvs/staticmodel/bin/python manage.py test`.
- Run `/Users/warren/work/virtualenvs/staticmodel/bin/sphinx-build -b doctest . _build/doctest` from `docs/`.
- Run a Sphinx HTML build from `docs/` to confirm Read the Docs rendering accepts the Markdown include.
- Confirm `git status --short` shows `README.rst` removed and `README.md` added.

## Assumptions
- `README.md` should be the single source of truth for public documentation.
- Read the Docs should remain live and render the README content through Sphinx.
- Long Python module docstrings should be replaced with short summaries, not kept as duplicate documentation.
- Existing executable examples should remain covered by doctest after the Markdown migration.
